<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Debug: allow cleartext (HTTP) to emulator host and LAN.
        For production release, remove base-config and use HTTPS.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
